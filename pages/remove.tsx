import type { NextPage } from "next";
import Head from "next/head";
import styles from "../styles/Home.module.css";
import { useState } from "react";
import { useRouter } from "next/router";

const RemoveTodos: NextPage = () => {
  const router = useRouter();
  const [isSaving, setIsSaving] = useState(false);

  const handleClick = async () => {
    setIsSaving(true);
    await fetch("/api/remove-all-todos", {
      method: "POST",
    });

    setIsSaving(false);
    router.push("/");
  };

  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={styles.main}>
        <h1 className={styles.title}>Remove ToDos</h1>

        <p>Click the button to remove all ToDos</p>

        <button onClick={handleClick}>Remove all ToDos</button>
        {isSaving && "Removing all ToDos..."}
      </main>
    </div>
  );
};

export default RemoveTodos;
